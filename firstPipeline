pipeline { 
    agent any
    environment{
    tool="jenkins"
}
    stages {
        stage("first Stage"){
            steps{
            
            echo "Hello World to shivam"    
 	    sh 'printenv'
            }
            
        }
    }
    
    post {
        always {
            echo "Post section of the pipeline"
        }
    }
}
