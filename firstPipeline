pipeline { 
    agent any
    environment{
    tool="jenkins"
    CREDS = credentials('Git Credentials')
}
    stages {
        stage("first Stage"){
            steps{
            
            echo "Hello World to shivam"    
 	    sh 'printenv'
            echo "Username is $CREDS_USR"
            }
            
        }
    }
    
    post {
        always {
            echo "Post section of the pipeline"
        }
    }
}
